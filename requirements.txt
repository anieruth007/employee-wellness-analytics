# PyTorch — install matching your CUDA version separately if this default doesn't fit your GTX 1650 driver, e.g.:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.1
torchvision>=0.16

# Landmark detection
mediapipe>=0.10

# FLIR E8 radiometric JPEG -> raw temperature array (also requires ExifTool installed
# system-wide: https://exiftool.org/)
flirimageextractor>=1.4

# Data / image handling
numpy>=1.24
pandas>=2.0
opencv-python>=4.8
Pillow>=10.0
scikit-learn>=1.3
scikit-image>=0.22

# Config / experiment tracking
PyYAML>=6.0
tensorboard>=2.15
tqdm>=4.66

# Dashboard
streamlit>=1.32
matplotlib>=3.8
plotly>=5.18

# Testing
pytest>=7.4
