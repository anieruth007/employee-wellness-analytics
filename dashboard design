Redesign the entire ThermaWell dashboard as a premium, visually stunning single-page web app. Replace dashboard/index.html completely. Pure HTML + CSS + JS only — no Streamlit, no Python backend. Use Chart.js from cdnjs for gauges. Here are the exact design requirements:

Design language:

Dark theme: background 
#080C14 (near black)
Card surfaces: 
#0D1422 with subtle border 
#1A2438
Accent: teal gradient 
#00C9A7 to 
#00A8E8
Danger: 
#FF4757
Warning: 
#FFA502
Success: 
#2ED573
Font: -apple-system, BlinkMacSystemFont, 'Segoe UI' — no imports needed
Glassmorphism cards: backdrop-filter blur with semi-transparent backgrounds
Subtle grid pattern on main background using CSS
All animations smooth — 60fps CSS transitions

Header (full width, sticky):

[🌡 ThermaWell]                    [● Live Analysis]  [Preview v1.0]
Thermal Physiological Monitoring
Left: logo with glowing teal thermometer icon (CSS drawn)
Center: subtle animated pulse dot indicating active session
Right: version badge with glass effect
Bottom border: thin teal gradient line

Hero section (below header):

Full width upload zone with dashed animated border
When no file: large centered upload icon + "Drop your FLIR thermal capture here"
When file uploaded: smoothly transitions to showing processed image
Processed image: displayed with green face bounding box overlay
ROI markers as colored dots (red=nose, yellow=forehead, cyan=periorbital, magenta=lip)
Below image: pill badges showing detector used + ambient temperature

Main scores section (3 cards side by side):

Each card is a glass-effect dark card with:

Glowing border matching the score color
Large circular Chart.js doughnut gauge (250px diameter)
Score number in center of gauge (48px bold)
Label below gauge
Colored status pill below label
Subtle pulsing glow animation on card border

Card 1 — Stress Index:

Gauge color: green→amber→red based on value
Value: 100
Status pill: 🔴 High
Subtitle: "Nasal thermal deviation"

Card 2 — Cognitive Load:

Gauge color: blue gradient (#00A8E8)
Value: 68
Status pill: 🔵 Very High
Subtitle: "Forehead thermal elevation"

Card 3 — Wellness Score:

Gauge color: teal (#00C9A7)
Value: 39.7
Status pill: 🟡 Needs Attention
Subtitle: "Combined physiological index"

All gauges animate from 0 to their value on load (1.2 second ease-out).

Alert banner (between scores and details):

⚠️  Elevated physiological stress detected
Stress markers are high alongside very high cognitive load.
Pushing through despite strain — consider a short break.
Red/amber gradient left border
Glass background
Subtle pulse animation on the warning icon

Details section (two columns):

Left column — ROI Data card:

Title: "Thermal ROI Analysis"
Two side-by-side mini tables:
Left: Raw temperatures (°C)
Right: Normalized deltas (°C above ambient)
Each row has a subtle colored dot matching the ROI color
Ambient reference shown below: "Ambient: 26.2°C"
Confidence bar:
Label: "Measurement Confidence"
Animated progress bar in teal
Value: 63% with text "Moderate — readings are reliable"

Right column — two stacked cards:

Card A — Interpretation:

Three mini stat rows:
Stress Level    [High]       red pill
Cognitive State [Very High]  blue pill
Wellness Flag   [Needs Attention] amber pill

Card B — Research Grounding:

📄 Stress basis
Fernández et al. (2024) — nose tip temperature
as stress index. Anxiety Stress Coping.

📄 Cognitive load basis
Frontiers in Psychiatry (2025) — forehead
thermal elevation under cognitive load.

📄 Autonomic marker
Gioia et al. (2023, Sensors) — differential
index as autonomic nervous system marker.

Each citation has a subtle left border in teal.

Footer:

🔒 Individual data stays on-device · Anonymous team averages only shared with management · DPDP Act 2023 compliant

ThermaWell Preview v1.0 · VIT Chennai · Research Demo
Phase 2: Employee wellness trends + Manager insights — coming soon

CSS animations to include:

css
/* Pulsing glow on score cards */
@keyframes cardGlow { 0%,100%{box-shadow:0 0 20px rgba(0,201,167,0.1)} 50%{box-shadow:0 0 40px rgba(0,201,167,0.3)} }

/* Animated dashed upload border */
@keyframes dashMove { to{stroke-dashoffset:-20} }

/* Warning pulse */
@keyframes warningPulse { 0%,100%{opacity:1} 50%{opacity:0.6} }

/* Gauge sweep from 0 */
/* Use Chart.js animation duration 1200ms easeOutQuart */

/* Confidence bar fill */
@keyframes barFill { from{width:0} to{width:63%} }

JavaScript behavior:

On load: show demo values from FLIR3643.jpg
Gauges animate from 0 to value on page load
File upload: show filename + image preview smoothly
Score card glow color changes based on value range
Add hover effects on all cards (slight lift + stronger glow)
Stress gauge needle color: green if < 40, amber if 40-70, red if > 70

After building:

Save as dashboard/index.html
Open in browser
Take full page screenshot
Commit with message: 'feat: ThermaWell premium dashboard redesign v2.0